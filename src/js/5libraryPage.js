function createLibraryCardFunc(imgPath, filmTitle, movieId, voteAverage) {
  //создает li согласно макета, вешает на нее слушателем функцию
  // activeDetailsPage c параметрами movieId и флагом true так как фильм из библиотеки
}

function drawQueueFilmList() {
  //
}

function drawWatchedFilmList() {
  //
}
